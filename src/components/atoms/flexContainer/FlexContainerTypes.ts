import { ReactChild } from 'react';

export type Directions = 'row' | 'reverse-row' | 'column' | 'reverse-column';

export type JustifyContent = 'center' | 'space-evenly' | 'space-around' | 'flex-start' | 'flex-end' | 'space-between';

export type AlignItems = 'center' | 'flex-start' | 'flex-end';
